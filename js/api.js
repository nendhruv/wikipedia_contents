export default class Map {
    constructor() {
        this.baseUrl = 'wikipedia.org/api/rest_v1/page/metadata/'
        this.fetchData();
    }

    fetchData() {
        console.log(baseUrl);
    }
}